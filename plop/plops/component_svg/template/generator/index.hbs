import * as React from "react"

type Props = {
  meta?: React.SVGProps<SVGSVGElement>
  color?: string
}

function Svg_{{ snakeCase name }}({
  color,
  meta,
}: Props) {
  return (
    
  )
}

export default Svg_{{ snakeCase name }}